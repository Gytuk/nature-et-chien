---
import SEO from "../components/SEO.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import "../styles/global.css";

interface Props {
  title: string;
  description: string;
  canonicalUrl?: string;
  ogImage?: string;
  ogImageWidth?: number;
  ogImageHeight?: number;
  noindex?: boolean;
  ogType?: string;
}

const { title, description, canonicalUrl, ogImage, ogImageWidth, ogImageHeight, noindex, ogType } = Astro.props;
---

<!doctype html>
<html lang="fr">
  <head>
    <SEO {title} {description} {canonicalUrl} {ogImage} {ogImageWidth} {ogImageHeight} {noindex} {ogType} />
  </head>
  <body>
    <Header />
    <main>
      <slot />
    </main>
    <Footer />
  </body>
</html>
